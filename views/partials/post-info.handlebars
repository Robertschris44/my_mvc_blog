<div>
    <h2>{{title}}</h2>
    <h3>{{user.username}}</h3>
    <a href="/post/{{id}}">{{post_content}}</a>

    <div>
        <h1>{{title}}</h1>
        <h4>Created by {{user.username}} on {{format_date created_at}}</h4>
    </div>

    <div>
        <h4>{{content}}</h4>
    </div>
</div>